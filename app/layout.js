import { Outfit } from "next/font/google";
import "./globals.css";
import { PlayerProvider } from "./music/context/PlayerContext"; // 👈 Import Provider
import MusicPlayer from "./music/components/MusicPlayer";

const outfit = Outfit({
  variable: "--font-outfit",
  subsets: ["latin"],
});

export const metadata = {
  title: "Echo: listen music flowlessly",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${outfit.variable} font-sans antialiased bg-white text-gray-900`}
      >
        {/* 🎧 Wrap the entire app */}
        <PlayerProvider>
          {children}
          <MusicPlayer />
        </PlayerProvider>
      </body>
    </html>
  );
}
